///
/// JHybridLocalWebViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridLocalWebViewStateUpdater.hpp"
#include "views/HybridLocalWebViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::localwebview::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridLocalWebViewState>;

void JHybridLocalWebViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridLocalWebViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridLocalWebViewSpec> hybridView = javaView->getJHybridLocalWebViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridLocalWebViewState& data = concreteState->getData();
  const std::shared_ptr<HybridLocalWebViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridLocalWebViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->assetsJson.isDirty) {
    hybridView->setAssetsJson(props->assetsJson.value);
    props->assetsJson.isDirty = false;
  }
  if (props->baseUrl.isDirty) {
    hybridView->setBaseUrl(props->baseUrl.value);
    props->baseUrl.isDirty = false;
  }
  if (props->cacheRequestJson.isDirty) {
    hybridView->setCacheRequestJson(props->cacheRequestJson.value);
    props->cacheRequestJson.isDirty = false;
  }
  if (props->configurationJson.isDirty) {
    hybridView->setConfigurationJson(props->configurationJson.value);
    props->configurationJson.isDirty = false;
  }
  if (props->documentId.isDirty) {
    hybridView->setDocumentId(props->documentId.value);
    props->documentId.isDirty = false;
  }
  if (props->html.isDirty) {
    hybridView->setHtml(props->html.value);
    props->html.isDirty = false;
  }
  if (props->sourceJson.isDirty) {
    hybridView->setSourceJson(props->sourceJson.value);
    props->sourceJson.isDirty = false;
  }
  if (props->onEvent.isDirty) {
    hybridView->setOnEvent(props->onEvent.value);
    props->onEvent.isDirty = false;
  }
  if (props->onShouldStartLoadWithRequest.isDirty) {
    hybridView->setOnShouldStartLoadWithRequest(props->onShouldStartLoadWithRequest.value);
    props->onShouldStartLoadWithRequest.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::localwebview::views
