///
/// HybridLocalWebViewCacheSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <NitroModules/Promise.hpp>
#include <vector>

namespace margelo::nitro::localwebview {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `LocalWebViewCache`
   * Inherit this class to create instances of `HybridLocalWebViewCacheSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridLocalWebViewCache: public HybridLocalWebViewCacheSpec {
   * public:
   *   HybridLocalWebViewCache(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridLocalWebViewCacheSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridLocalWebViewCacheSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridLocalWebViewCacheSpec() override = default;

    public:
      // Properties
      virtual std::string getDocumentsDirectory() = 0;

    public:
      // Methods
      virtual void cancelDownload(const std::string& requestId) = 0;
      virtual std::shared_ptr<Promise<void>> copyFile(const std::string& source, const std::string& destination) = 0;
      virtual std::shared_ptr<Promise<std::string>> download(const std::string& requestId, const std::string& requestJson) = 0;
      virtual std::shared_ptr<Promise<bool>> exists(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<std::string>> hashFile(const std::string& path, const std::string& algorithmsJson) = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> listDirectory(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> makeDirectory(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> moveFile(const std::string& source, const std::string& destination) = 0;
      virtual std::shared_ptr<Promise<std::string>> readFile(const std::string& path, const std::string& encoding) = 0;
      virtual std::shared_ptr<Promise<std::string>> readFileRange(const std::string& path, double start, double end, const std::string& encoding) = 0;
      virtual std::shared_ptr<Promise<void>> remove(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<double>> stat(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> writeFile(const std::string& path, const std::string& value, const std::string& encoding) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "LocalWebViewCache";
  };

} // namespace margelo::nitro::localwebview
