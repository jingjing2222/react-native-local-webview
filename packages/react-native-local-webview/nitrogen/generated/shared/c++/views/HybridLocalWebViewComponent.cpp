///
/// HybridLocalWebViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridLocalWebViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::localwebview::views {

  extern const char HybridLocalWebViewComponentName[] = "LocalWebView";

  HybridLocalWebViewProps::HybridLocalWebViewProps(const react::PropsParserContext& context,
                                                   const HybridLocalWebViewProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    assetsJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("assetsJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.assetsJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.assetsJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.assetsJson: ") + exc.what());
      }
    }()),
    baseUrl([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("baseUrl", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.baseUrl;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.baseUrl);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.baseUrl: ") + exc.what());
      }
    }()),
    cacheRequestJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("cacheRequestJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.cacheRequestJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.cacheRequestJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.cacheRequestJson: ") + exc.what());
      }
    }()),
    configurationJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("configurationJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.configurationJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.configurationJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.configurationJson: ") + exc.what());
      }
    }()),
    documentId([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("documentId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.documentId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.documentId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.documentId: ") + exc.what());
      }
    }()),
    html([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("html", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.html;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.html);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.html: ") + exc.what());
      }
    }()),
    sourceJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.sourceJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.sourceJson: ") + exc.what());
      }
    }()),
    onEvent([&]() -> CachedProp<std::function<void(const std::string& /* event */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onEvent", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onEvent;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const std::string& /* event */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onEvent);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.onEvent: ") + exc.what());
      }
    }()),
    onShouldStartLoadWithRequest([&]() -> CachedProp<std::function<std::shared_ptr<Promise<bool>>(const std::string& /* request */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onShouldStartLoadWithRequest", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onShouldStartLoadWithRequest;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<std::shared_ptr<Promise<bool>>(const std::string& /* request */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onShouldStartLoadWithRequest);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.onShouldStartLoadWithRequest: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridLocalWebViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridLocalWebViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LocalWebView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridLocalWebViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("assetsJson"): return true;
      case hashString("baseUrl"): return true;
      case hashString("cacheRequestJson"): return true;
      case hashString("configurationJson"): return true;
      case hashString("documentId"): return true;
      case hashString("html"): return true;
      case hashString("sourceJson"): return true;
      case hashString("onEvent"): return true;
      case hashString("onShouldStartLoadWithRequest"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridLocalWebViewComponentDescriptor::HybridLocalWebViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridLocalWebViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
                                                                                        react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridLocalWebViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridLocalWebViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridLocalWebViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridLocalWebViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridLocalWebViewProps>& props = std::const_pointer_cast<HybridLocalWebViewProps>(constProps);
    HybridLocalWebViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::localwebview::views
